const longLine = "This line is supposed" + "to be longer than" + "80 characters";

const longestLine = "This line is supposed" + "to be even longer  than longer than" + "100 characters";

const myObject = {
    a: "this should single quotes",
    b: "this should get a trailing comma"
} // this should get a semi-colon